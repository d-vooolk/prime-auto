/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // Включает строгий режим React
    output: 'standalone', // Для удобного развертывания в продакшене
};

export default nextConfig;
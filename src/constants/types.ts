interface MetaParams {
    title: string;
    description: string;
    keywords: string[];
    robots: string[];
}

export interface MetaParamsInterface {
    general: MetaParams,
    uslugi: MetaParams,
    lightQuality: MetaParams,
}